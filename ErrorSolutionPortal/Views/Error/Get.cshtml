@model ErrorSolutionPortal.Entities.ErrorSolution

@{
    ViewData["Title"] = "Get";
}
<h1>
    Error :
    <br />@Model.Name
</h1>

<div role="form">
    <div>
        <fieldset>
            @*<div class="form-group">
                <label for="txtProcess">Name: </label>
                @Html.DisplayFor(m => m.Name, new { @class = "form-control" })
            </div>*@
            <div class="form-group">
                <label for="txtProcess">Description: </label>
                @Html.DisplayFor(m => m.Description, new { @class = "form-control" })
            </div>
            <div class="form-group">
                <label for="txtProcess">Solutions: </label>
                @Html.DisplayFor(m => m.Solution, new { @class = "form-control" })
            </div>
            <div class="form-group">
                <button type="button" class="btn btn-primary directory-search">Ok</button>
                <button type="button" class="btn btn-secondary directory-reset">Cancel</button>
            </div>
        </fieldset>
    </div>
</div>
